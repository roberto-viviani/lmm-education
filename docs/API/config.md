# config

The config module in LM markdown for education contains a main class ConfigSettings, which inherits from Config in LM markdown. As a result, when written or read to config.toml, sections and properties of both are included.

::: lmm_education.config.config